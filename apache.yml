---
- hosts: nodes
  tasks:
  - name: Installing Apache
    yum:
      name: httpd
      state: installed
    become: yes
   - name: starting Apache
    service:
      name: httpd
      state: started
    become: yes
